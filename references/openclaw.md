# OpenClaw Adapter

- Inspect the active OpenClaw skill roots and loading precedence before deployment.
- Keep the shared skill frontmatter portable. Put OpenClaw-only fields in a separate package only when required.
- Verify the configured TTS or media provider independently from the agent's reasoning-model authentication.
- Restart or open a fresh session when cached skill metadata prevents discovery testing.
- Use the organization's Skill Workshop approval workflow when it is enabled.
- Verify the skill with both a natural-language audio-production request and a negative visual/video request.
